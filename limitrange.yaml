apiVersion: v1
kind: LimitRange
metadata:
  name: limit-range-production
  namespace: production
spec:
  limits:
    - type: Pod
      default:
        cpu: "500m"
        memory: "256Mi"
      defaultRequest:
        cpu: "250m"
        memory: "128Mi"
